/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['www.pexels.com'], // Add the allowed domains for `next/image`
    },
  };
  
  export default nextConfig;
  